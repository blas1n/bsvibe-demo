# toolkit

A small, well-tested Python utility toolkit — built and maintained with BSVibe.

Every change ships with tests and passes the CI gate (`ruff` + `pytest`).

## Install (dev)

```sh
uv sync
```

## Test

```sh
ruff check .
pytest -q
```
